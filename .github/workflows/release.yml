name: Build & Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET 8
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Build — Apple Music RPC (core)
        run: |
          dotnet publish src/AppleMusicRPC/AppleMusicRPC.csproj `
            -c Release `
            -r win-x64 `
            --self-contained true `
            -p:PublishSingleFile=true `
            -p:PublishTrimmed=true `
            -p:TrimMode=partial `
            -p:IncludeNativeLibrariesForSelfExtract=true `
            -o publish/core

      - name: Build — Updater
        run: |
          dotnet publish src/Updater/Updater.csproj `
            -c Release `
            -r win-x64 `
            --self-contained true `
            -p:PublishSingleFile=true `
            -o publish/updater

      - name: Build — Setup
        run: |
          dotnet publish src/Setup/Setup.csproj `
            -c Release `
            -r win-x64 `
            --self-contained true `
            -p:PublishSingleFile=true `
            -o publish/setup

      - name: Create AppleMusicRPC.zip (payload de mise à jour)
        run: |
          Compress-Archive `
            -Path "publish/core/Apple Music RPC.exe" `
            -DestinationPath "publish/AppleMusicRPC.zip"

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          name: "Apple Music RPC ${{ github.ref_name }}"
          body: |
            ## Installation (première fois)
            1. Télécharge **`Apple Music RPC Setup.exe`**
            2. Double-clique — il installe tout automatiquement
            3. Discord affiche ta musique Apple Music en temps réel ✓

            ## Mise à jour
            L'updater vérifie les nouvelles versions automatiquement à chaque démarrage Windows.

            ## Désinstallation
            Panneau de configuration → Programmes → **Apple Music RPC** → Désinstaller
            *ou* clic droit sur l'icône dans la barre des tâches → Désinstaller
          files: |
            publish/setup/Apple Music RPC Setup.exe
            publish/updater/Apple Music RPC Updater.exe
            publish/AppleMusicRPC.zip
          draft: false
          prerelease: false
